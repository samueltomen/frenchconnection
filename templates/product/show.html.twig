{% extends 'base.html.twig' %}

{% block title %}
    {{ product.name }} - French Connection
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-4">
            <img src="/uploads/{{ product.illustration }}"
                alt="{{ product.name }}"
                class="img-fluid" />
        </div>
        <div class="col-md-8">
            <h3>
                {{ product.name }}
            </h3>
            <p>
                {{ product.subtitle }}
            </p>
            <p class="product-page-price">
                {{ (product.price / 100)|number_format(2) }} €
            </p>
            <hr />
            <p>
                {{ product.description }}
            </p>
            <hr />
            <a href="" class="btn btn-primary">Ajouter au panier</a>
        </div>
    </div>
{% endblock %}
